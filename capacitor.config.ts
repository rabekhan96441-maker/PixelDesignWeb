import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khanrabe.pixeldesignweb',
  appName: 'Pixel Design',
  webDir: 'dist'
};

export default config;
